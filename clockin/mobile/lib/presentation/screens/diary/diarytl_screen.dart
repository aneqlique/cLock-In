import 'package:flutter/material.dart';

class DiarytlScreen extends StatelessWidget {
  const DiarytlScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Diary Timeline',
        style: TextStyle(fontSize: 18, fontWeight: FontWeight.w600),
      ),
    );
  }
}
